/**
 * The class {@code ${NAME}} Translates strings in matrix form into
 * <p>different types of a matrix like {@code Boolean}, {@code Integer}, {@code Double}.
 *
 * @author ${USER}
 * @version 0.1
#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "") * @see ${PACKAGE_NAME}
#end
 * @see String
 * @since 1.0
 */
// @created ${DATE}, ${DAY_NAME_FULL}